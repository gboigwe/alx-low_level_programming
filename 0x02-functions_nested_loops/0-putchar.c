#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[] = "_putchar\n";

	for (int a = 0; str[a] != 0; a++)
	{
		_putchar(str[]);
	}
	return (0);
}
